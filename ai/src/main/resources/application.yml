server:
  port: 8090    # 服务端口，默认8080，可自定义

spring:
  application:
    name: aki
  threads:
    virtual:
      enabled: true # 启用虚拟线程
  cache:
    type: none
    cache-names: global_memory

  ai:
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
    openai:
      chat:
        options:
          model: deepseek-ai/DeepSeek-V3
      base-url: https://api.siliconflow.cn/v1
      api-key: 1
##数据库
  datasource:
    url: jdbc:mysql://39.105.125.7:3306/aki?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&serverTimezone=Asia/Shanghai
    username: root
    password: a2133266
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 10
# 日志级别（可选）
logging:
  level:
    root: info

# Aki应用配置
aki:
  llm:
    default-character-id: bartender  # 默认角色ID，可配置
  debug:
    # 提示词捕获配置
    prompt-capture:
      enabled: true  # 是否启用提示词捕获
      max-length: 500  # 最大显示长度
      include-sensitive: false  # 是否包含敏感信息

sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: token
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  jwt-secret-key: asounfuryfhueuiwyurfewbfjsdafjk

